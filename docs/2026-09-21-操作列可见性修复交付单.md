# 操作列可见性修复交付单

## 变更内容

- 底座 `OperationColumnActions` 在排列与折叠前统一排除空项、显式 hidden 项、无权限且隐藏的项，展开 Fragment 内动作；无权限但明确配置 disabled 模式的入口保留位置。
- 保持现有规则：不足四项全部展示，四项及以上前两项直接显示，其余进入“更多”。业务状态仍由调用方条件渲染；公共组件不推断子组件内部的业务状态。
- 工作台数据展示分类新增三组示例：隐藏和无权限项补位、分组折叠、不足四项与禁用入口。
- 接入规则明确在调用处表达业务可见性；组件审计补齐条件表达式和 Fragment 内的文字动作检查，防止条件渲染绕过已有规范。
- 接口、权限值、菜单及数据库结构均无变更，不需要表结构确认或新增 migration。
- 本次仅修改 project-template；复用并核对 SIDM 已有过滤实现，未修改 SIDM 仓库。

## 自动门禁

- `node scripts/verify-change.mjs`：通过。前端 311 项通过；后端 91 项通过、1 项按环境条件跳过；脚本测试 25 项通过。
- lint、组件普通/严格审计、接口契约、构建、构建体积、操作历史审计及后端运行态新鲜度：通过。
- 回归测试已在旧逻辑下失败、修复后通过，覆盖隐藏、权限、分组、空项、禁用保留以及业务条件渲染防绕过。
- `git diff --check`：通过。
- 原有本地 PostgreSQL 容器停止，已启动现有容器，未重建或清空数据。后端健康检查 200，数据库 connected；前端 3102 返回 200；migration 只读检查无待执行项。
- 首轮沙箱无法读取进程启动时间，获得本机命令权限后完整重跑门禁通过。

## 验收

- 地址：http://localhost:3102/system/design-system?category=display
- 用户明确授权本地 admin 登录验收后，浏览器已使用有效管理员会话进入工作台并完成实际检查。此前的登录授权阻塞已解除。
- 工作台实测通过：第一组直接显示“编辑、跟进记录、更多”，点击更多仅含“复制、查看历史”；隐藏及无权限项不占位且无重复动作。第二组全部三项直显、无更多。第三组“受限操作”实际 disabled 且保留位置。
- 真实工单列表实测通过：操作列显示“编辑、状态变更、更多”，更多包含“复制、删除”。实际打开状态变更和删除确认弹窗后取消，未提交状态变化或删除。
- 查询不存在关键词显示“暂无数据”、0 条；重置恢复 61 条；下一页显示第 21–40 条且操作列正常。已观察真实页面截图，文字动作布局正常；创建人、创建时间位于操作列前。
- 页面语义、列表布局、标签、历史、状态矩阵、菜单授权规则均沿用现有实现，本次没有调整这些业务契约，未宣称逐项浏览器复验完成。

## 交付状态

- 代码已修改，自动门禁通过，本次范围浏览器冒烟验收完成；待用户最终业务验收。未提交、未推送、未发布。
- 保留工作区原有三份 superpowers 文档删除，不纳入本次修复。
